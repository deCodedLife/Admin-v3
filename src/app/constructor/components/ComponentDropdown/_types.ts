import { TComponentButton } from "../ComponentButton/_types";

export type TActionsDropdown = { children: any, onClick: (value: any) => void }

export type TComponentDropdown = { buttons: Array<TComponentButton> }