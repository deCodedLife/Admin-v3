import { TComponentButton } from "../../components/ComponentButton/_types";
import { TDefaultModule } from "../_types";

export type TModuleButtons = TDefaultModule<"buttons", { buttons: Array<TComponentButton> }>